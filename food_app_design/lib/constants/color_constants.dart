import 'package:flutter/material.dart';

class ColorConstants {
  static final ColorConstants instance = ColorConstants._init();
  ColorConstants._init();

  final Color iconBackgroundColor = Colors.amber;
  final Color navbarBackgroundColor = Colors.grey[200];
  final Color backgroundWhite = Colors.white;
}
